server:
  port: 8084

spring:
  application:
    name: STUDENT-SERVICE
  data:
    mongodb:
      uri: mongodb://localhost:27017/avaliakids-students

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
